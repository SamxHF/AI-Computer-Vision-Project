# Loaded by VS Code / Python extension when "python.envFile" points here.
# Prevents TensorFlow from picking up a mismatched user-site install under
# ~/Library/Python/3.9/... (e.g. broken tensorflow-plugins / libmetal_plugin.dylib).
PYTHONNOUSERSITE=1
